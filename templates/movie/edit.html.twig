{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('app_movie_list') }}"> <<< </a>
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.release_year) }}
        {{ form_row(form.genres) }}
        {{ form_row(form.description) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}

{% endblock %}